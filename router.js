export function createRouter({routes,onRender}){let stack=['dashboard'];const nav=document.querySelector('#nav'),page=document.querySelector('#page'),crumb=document.querySelector('#breadcrumb');
 nav.innerHTML=routes.map(r=>`<button class="nav-button" data-route="${r.id}">${r.icon} ${r.label}</button>`).join('');
 function go(id,push=true){const route=routes.find(r=>r.id===id)||routes[0];document.querySelectorAll('.nav-button').forEach(b=>b.classList.toggle('active',b.dataset.route===route.id));crumb.textContent=route.label;if(push&&stack.at(-1)!==route.id)stack.push(route.id);page.innerHTML=route.render();onRender?.(route.id);document.querySelector('#appShell').classList.remove('menu-open');page.focus();scrollTo(0,0)}
 nav.addEventListener('click',e=>{const b=e.target.closest('[data-route]');if(b)go(b.dataset.route)});document.querySelector('#homeButton').onclick=()=>{stack=['dashboard'];go('dashboard',false)};document.querySelector('#backButton').onclick=()=>{if(stack.length>1)stack.pop();go(stack.at(-1)||'dashboard',false)};return{go,back(){document.querySelector('#backButton').click()},current:()=>stack.at(-1)}}
